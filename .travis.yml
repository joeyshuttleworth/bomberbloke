#Used by travis CI for testing
dist: bionic

language: cpp

compiler:
  - gcc
  - clang
  
before_install:
  - sudo apt-get install -qq libsdl2-dev libenet-dev libcereal-dev

before_script:
  - cmake .

script:
  - cmake --build .
  - ctest --verbose
